<nav class="nav">
    <div class="nav__content-nav">
        <img src="{{ asset('img/logo.png') }}" alt="logo de Chiirz">
        
        <img src="{{ asset('img/svg/responsive-menu-btn.svg') }}" alt="burger-menu">

        <ul class="p p-20">
            <li> 
                <a href="{{ path('home') }}">Accueil</a>
            </li>
            <li> 
                {% if app.user %}
                    <a href="{{ path('app_city_show', {id : app.user.id}) }}">Itinéraires</a>
                {% else %}
                    <a href="{{ path('app_city_index') }}">Itinéraires</a>
                {% endif %}
            </li>
        </ul>   
    </div>
    {% if app.user %}
        <a href="{{ path('user', {'id': app.user.id }) }}">
            {{ app.user.pseudo }}
        </a>
    {% else %}
        <div class="nav__content-btn p p--16">
            <a href="{{ path('login') }}" >Se connecter </a>
            <a href="{{ path('register') }}" class="btn btn--secondary">S'inscrire </a>
        </div>
    {% endif %}
</nav>